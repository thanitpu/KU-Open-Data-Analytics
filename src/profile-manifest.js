// KU Open DA — local Profile Manifest v1. Aggregated statistics only; no raw rows leave the browser.
(function(root,factory){
  const api=factory();
  if(typeof module==='object'&&module.exports)module.exports=api;
  if(root)root.KUProfileManifest=api;
})(typeof window!=='undefined'?window:globalThis,function(){
'use strict';
const VERSION='1.0';
const missing=v=>v===''||v===null||v===undefined||(typeof v==='number'&&Number.isNaN(v));
const finite=v=>{const n=Number(v);return Number.isFinite(n)?n:null};
const mean=a=>a.length?a.reduce((s,v)=>s+v,0)/a.length:NaN;
function quantile(a,p){if(!a.length)return NaN;const s=[...a].sort((x,y)=>x-y),i=(s.length-1)*p,lo=Math.floor(i),hi=Math.ceil(i);return lo===hi?s[lo]:s[lo]+(s[hi]-s[lo])*(i-lo)}
function sampleSD(a){if(a.length<2)return NaN;const m=mean(a);return Math.sqrt(a.reduce((s,v)=>s+(v-m)**2,0)/(a.length-1))}
function skewness(a){const n=a.length,m=mean(a),sd=sampleSD(a);if(n<3||!sd)return 0;return n/((n-1)*(n-2))*a.reduce((s,v)=>s+((v-m)/sd)**3,0)}
function excessKurtosis(a){const n=a.length,m=mean(a),sd=sampleSD(a);if(n<4||!sd)return 0;const z=a.reduce((s,v)=>s+((v-m)/sd)**4,0);return n*(n+1)/((n-1)*(n-2)*(n-3))*z-3*(n-1)**2/((n-2)*(n-3))}
function medianAbsoluteDeviation(a){if(!a.length)return NaN;const med=quantile(a,.5);return quantile(a.map(v=>Math.abs(v-med)),.5)}
function histogram(a,bins=12){if(!a.length)return{bins:[],edges:[]};const lo=Math.min(...a),hi=Math.max(...a);if(lo===hi)return{bins:[a.length],edges:[lo,hi]};const k=Math.max(4,Math.min(30,Number(bins)||12)),w=(hi-lo)/k,counts=Array(k).fill(0);for(const v of a){const i=Math.min(k-1,Math.floor((v-lo)/w));counts[i]++}return{bins:counts,edges:Array.from({length:k+1},(_,i)=>lo+i*w)}}
function shapeLabel(sk,sd){if(!Number.isFinite(sd)||sd===0)return'constant';if(sk>=2)return'strong_right_skew';if(sk>=1)return'right_skew';if(sk>=.5)return'mild_right_skew';if(sk<=-2)return'strong_left_skew';if(sk<=-1)return'left_skew';if(sk<=-.5)return'mild_left_skew';return'roughly_symmetric'}
function numericProfile(values,bins){const a=values.map(finite).filter(v=>v!==null),n=a.length;if(!n)return null;const q1=quantile(a,.25),med=quantile(a,.5),q3=quantile(a,.75),iqr=q3-q1,lower=q1-1.5*iqr,upper=q3+1.5*iqr,out=a.filter(v=>v<lower||v>upper),mad=medianAbsoluteDeviation(a),madOut=mad>0?a.filter(v=>Math.abs(.6745*(v-med)/mad)>3.5):[];const sd=sampleSD(a),sk=skewness(a),ku=excessKurtosis(a);return{summary:{min:Math.min(...a),q1,median:med,q3,max:Math.max(...a),mean:mean(a),sd,skewness:sk,excess_kurtosis:ku,zero_pct:100*a.filter(v=>v===0).length/n,negative_pct:100*a.filter(v=>v<0).length/n},distribution:{shape:shapeLabel(sk,sd),histogram:histogram(a,bins)},outliers:{method_iqr:{lower,upper,count:out.length,pct:100*out.length/n},method_mad:{count:madOut.length,pct:100*madOut.length/n}}}}
function entropyNormalized(counts){const vals=[...counts.values()],n=vals.reduce((s,v)=>s+v,0),k=vals.length;if(!n||k<=1)return 0;const h=-vals.reduce((s,c)=>{const p=c/n;return s+(p? p*Math.log(p):0)},0);return h/Math.log(k)}
function sensitiveLike(name){const s=String(name||'').toLowerCase().replace(/[^a-z0-9]+/g,'_');return/(^|_)(email|e_mail|phone|mobile|address|first_name|last_name|full_name|customer_name|person_name|national_id|passport)(_|$)/.test(s)}
function structuralLike(name,n,unique){const s=String(name||'').toLowerCase();return['id','customer_id','row_id','index','uuid'].includes(s)||(n>=20&&unique/Math.max(1,n)>=.98)}
function categoricalProfile(values,name,maxCategories){const vals=values.map(v=>String(v)),counts=new Map();for(const v of vals)counts.set(v,(counts.get(v)||0)+1);const entries=[...counts.entries()].sort((a,b)=>b[1]-a[1]||a[0].localeCompare(b[0])),n=vals.length,rareLimit=Math.max(2,Math.floor(n*.01)),rare=entries.filter(([,c])=>c<=rareLimit),redacted=sensitiveLike(name)||structuralLike(name,n,entries.length);return{redacted,redacted_reason:redacted?'identifier_or_sensitive_like_field':null,top:redacted?[]:entries.slice(0,maxCategories).map(([value,count])=>({value,count,pct:100*count/n})),other_count:redacted?null:entries.slice(maxCategories).reduce((s,[,c])=>s+c,0),dominant_pct:entries.length?100*entries[0][1]/n:0,rare_level_count:rare.length,rare_observation_pct:100*rare.reduce((s,[,c])=>s+c,0)/Math.max(1,n),entropy_normalized:entropyNormalized(counts)}}
function temporalNameHint(name){return/(^|_)(date|dt|time|timestamp)(_|$)/i.test(String(name||''))||/(date|timestamp)$/i.test(String(name||''))}
function temporalProfile(values,name,storage){const vals=values.filter(v=>!missing(v));if(!vals.length)return null;if(storage==='numeric'&&!temporalNameHint(name))return null;const isoLike=vals.filter(v=>typeof v==='string'&&/^\d{4}[-/]\d{1,2}[-/]\d{1,2}/.test(v.trim())).length;const hinted=temporalNameHint(name)||isoLike/vals.length>=.5;if(!hinted)return null;const parsed=vals.map(v=>Date.parse(v)).filter(Number.isFinite);if(parsed.length/vals.length<.8)return null;const uniq=[...new Set(parsed)].sort((a,b)=>a-b),diffs=uniq.slice(1).map((v,i)=>(v-uniq[i])/86400000).filter(v=>v>0),med=diffs.length?quantile(diffs,.5):null,m=diffs.length?mean(diffs):null,sd=diffs.length>1?sampleSD(diffs):null,cv=m&&Number.isFinite(sd)?sd/m:null;let granularity='single_point';if(med!==null){if(med<1)granularity='subdaily';else if(med<=2)granularity='daily';else if(med<=8)granularity='weekly';else if(med<=40)granularity='monthly';else if(med<=100)granularity='quarterly';else if(med<=400)granularity='yearly';else granularity='irregular_long_interval'}return{detected:true,parse_rate:parsed.length/vals.length,min:new Date(Math.min(...parsed)).toISOString(),max:new Date(Math.max(...parsed)).toISOString(),unique_timestamps:uniq.length,median_interval_days:med,interval_cv:cv,granularity}}
function duplicateRows(headers,data){const seen=new Set();let dup=0;for(const row of data){const key=headers.map(h=>String(row[h]??'')).join('\u001f');if(seen.has(key))dup++;else seen.add(key)}return dup}
function build({headers=[],data=[],types={},meta={},analysisIntent={},maxCategories=20,histogramBins=12}={}){if(!Array.isArray(headers)||!Array.isArray(data))throw new Error('headers and data must be arrays');const fields=[];let missingCells=0;for(const name of headers){const observed=data.map(r=>r?.[name]).filter(v=>!missing(v)),miss=data.length-observed.length,unique=new Set(observed.map(v=>String(v))).size,storage=types?.[name]||((observed.every(v=>finite(v)!==null)&&observed.length)?'numeric':'text'),level=meta?.[name]?.level||null;missingCells+=miss;const numeric=storage==='numeric'?numericProfile(observed,histogramBins):null,temporal=temporalProfile(observed,name,storage),role=analysisIntent?.target===name?'target':'predictor';const field={name,role,storage_type:storage,measurement_level:level,profile:{n:observed.length,missing:miss,missing_pct:data.length?100*miss/data.length:0,unique,unique_ratio:observed.length?unique/observed.length:0}};if(numeric){Object.assign(field.profile,numeric.summary);field.distribution=numeric.distribution;field.outliers=numeric.outliers}else field.frequency=categoricalProfile(observed,name,maxCategories);if(temporal)field.temporal=temporal;field.privacy={frequency_values_redacted:Boolean(field.frequency?.redacted)};fields.push(field)}const totalCells=data.length*headers.length;return{schema_version:VERSION,generated_by:'browser',privacy:{row_level_values_included:false,categorical_frequency_limit:maxCategories},analysis_intent:{...analysisIntent},dataset_profile:{rows:data.length,fields:headers.length,missing_cells:missingCells,missing_pct:totalCells?100*missingCells/totalCells:0,duplicate_rows:duplicateRows(headers,data),numeric_fields:fields.filter(f=>f.storage_type==='numeric').length,categorical_fields:fields.filter(f=>f.storage_type!=='numeric'&&!f.temporal).length,temporal_fields:fields.filter(f=>f.temporal?.detected).length},fields}}
function fromGlobals(analysisIntent={}){if(typeof headers==='undefined'||typeof data==='undefined')throw new Error('Dataset globals are unavailable.');return build({headers,data,types:typeof types==='undefined'?{}:types,meta:typeof meta==='undefined'?{}:meta,analysisIntent})}
return{VERSION,build,fromGlobals};
});
