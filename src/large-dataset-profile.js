// KU Open DA — visible provenance for browser large-dataset profiling.
(function(root){
'use strict';
const LIMIT=100000;
let fullDataRef=null;
function loaded(){return typeof data!=='undefined'&&Array.isArray(data)&&data.length>0}
function ensureStyle(){if(document.getElementById('largeProfileStyle'))return;const s=document.createElement('style');s.id='largeProfileStyle';s.textContent='.large-profile-mode{margin:10px 0 14px;padding:11px 13px;border:1px solid #CDE0DE;border-left:4px solid var(--ku-green);border-radius:11px;background:var(--ku-soft-green);display:flex;gap:10px;align-items:flex-start;justify-content:space-between}.large-profile-mode b{color:var(--ku-green);font-size:11px}.large-profile-mode span{display:block;margin-top:3px;color:var(--ku-muted);font-size:10.5px;line-height:1.5}.large-profile-mode em{font-style:normal;white-space:nowrap;font-size:10px;font-weight:800;color:var(--ku-green);background:#fff;border:1px solid #CDE0DE;border-radius:999px;padding:5px 8px}aside .advanced-note,aside .advanced-nav{display:none!important}body>header .logo{background:transparent!important;box-shadow:none!important;border-radius:0!important}@media(max-width:650px){.large-profile-mode{display:grid}.large-profile-mode em{justify-self:start}}';document.head.appendChild(s)}
function activeRows(){return fullDataRef||data}
function render(){ensureStyle();const view=document.getElementById('variablesView');if(!view)return;const source=loaded()?activeRows():null;let box=document.getElementById('largeProfileMode');if(!source||source.length<=LIMIT){box?.remove();return}if(!box){box=document.createElement('div');box.id='largeProfileMode';box.className='large-profile-mode';const band=document.getElementById('profileDatasetBand');if(band)band.insertAdjacentElement('afterend',box);else view.prepend(box)}const fraction=100*LIMIT/source.length;box.innerHTML=`<div><b>Large Dataset Mode</b><span>${source.length.toLocaleString()} rows × ${typeof headers!=='undefined'?headers.length:'—'} fields. Distribution, outlier and categorical diagnostics use a deterministic representative sample of ${LIMIT.toLocaleString()} rows (${fraction.toFixed(1)}%) for responsive browser exploration. The uploaded dataset itself is not reduced.</span></div><em>Profile basis · ${LIMIT.toLocaleString()} sampled rows</em>`}
function alignProfileTop(){const view=document.getElementById('variablesView');if(!view||view.classList.contains('hidden'))return;const header=document.querySelector('body > header');const top=view.getBoundingClientRect().top+window.scrollY-(header?.offsetHeight||0)-8;window.scrollTo({top:Math.max(0,top),behavior:'auto'})}
function restoreFullData(){if(fullDataRef){data=fullDataRef;fullDataRef=null}}
function enterAnalyzeSample(){if(fullDataRef||!loaded()||data.length<=LIMIT)return;if(root.KUProfileManifest?.deterministicSample){fullDataRef=data;data=root.KUProfileManifest.deterministicSample(fullDataRef,LIMIT).rows}}
function installAnalyzeBridge(){const rawShow=root.showAIAnalyticsView;if(typeof rawShow==='function'&&!rawShow.__kuLargeDatasetBridge){const wrapped=function(...args){enterAnalyzeSample();try{return rawShow.apply(this,args)}catch(err){restoreFullData();throw err}};wrapped.__kuLargeDatasetBridge=true;root.showAIAnalyticsView=wrapped}const rawGo=root.goToJourneyStep;if(typeof rawGo==='function'&&!rawGo.__kuLargeDatasetBridge){const wrapped=function(step,...args){if(step!=='analyze')restoreFullData();return rawGo.call(this,step,...args)};wrapped.__kuLargeDatasetBridge=true;root.goToJourneyStep=wrapped}document.addEventListener('click',e=>{const step=e.target.closest?.('[data-journey-step]')?.dataset?.journeyStep;if(step&&step!=='analyze')restoreFullData()},{capture:true})}
function install(){render();const status=document.getElementById('status');if(status&&typeof MutationObserver!=='undefined')new MutationObserver(()=>setTimeout(render,0)).observe(status,{childList:true,subtree:true,characterData:true});document.addEventListener('ku:statechange',()=>setTimeout(render,0));document.addEventListener('click',e=>{if(e.target.closest?.('[data-journey-step="profile"]'))setTimeout(render,0)});setTimeout(installAnalyzeBridge,0)}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',install);else install();
root.KULargeDatasetProfile={LIMIT,render,alignProfileTop,restoreFullData};
})(window);
